# Normalize line endings: everything is text, Lua/MD/XML stay LF on checkout
# (MTA and cross-platform tooling expect LF; prevents CRLF churn on Windows).
* text=auto
*.lua text eol=lf
*.md  text eol=lf
*.xml text eol=lf
*.py  text eol=lf