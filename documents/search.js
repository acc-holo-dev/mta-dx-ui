// generated by documents/gen.lua — the embedded search indexvar WIKI_INDEX = [
    { u: "widgets.html", t: "Widgets", x: " Widgets factory reference Every widget is created through its ui:&lt;name&gt;(props) factory. Factories are synthesized from the widget registry: any registered class gains one automatically. Property tables below are REFLECTED from the live spec tables — they are always exact. Inherited base properties (x, y, width, height, visible, opacity, color, font, layer, zIndex, clip, margin, padding, layoutMode, interactive, focusable, enabled, textKey ...) are documented in Concepts and omitted here. Composites expose their content slot via node:container() . ui:panel(props) Panel Static surface — the base of every bordered widget. property spec default blur 0 borderColor nil borderWidth 1 effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 justify nil mask nil radius 0 shrink number, min 0 0 local p = ui:panel({ x=0, y=0, width=100, height=60, radius=8 }) ui:label(props) Label Auto-sizing text with wrap/ellipsis and translation binding. property spec default blur 0 effect nil ellipsis false flexDirection validate nil gap number, min 0 0 grow number, min 0 0 justify nil mask nil shadow transform nil shrink number, min 0 0 textColor transform 4294967295 0xFFFFFFFF local l = ui:label({ x=0, y=0, text=\"Hello\" }) l:setTextKey(\"greeting\") ui:button(props) Button Clickable surface with hover/pressed/focused states and themed colors. Events: click property spec default blur 0 borderColor nil borderWidth 1 effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 justify nil mask nil radius 0 shrink number, min 0 0 textColor transform 4294967295 0xFFFFFFFF local btn = ui:button({ x=0, y=0, width=100, height=28, text=\"OK\" }) btn:on(\"click\", function() save() end) ui:image(props) Image Texture quad with sections, color tint and effects (blur/mask). property spec default blur 0 effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 justify nil mask nil section nil shrink number, min 0 0 texture nil local i = ui:image({ x=0, y=0, width=64, height=64, texture=\"icons/x.png\" }) ui:window(props) Window Titled panel: the header DRAGS the window (clamped so it stays reachable), a close-button part (a real themed Button) emits \"close\", children live in the content part. Events: close property spec default blur 0 borderColor nil borderWidth 1 closeButtonText onSet \"xD7\" closeButtonVisible onSet true draggable true effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 headerHeight onSet 28 justify nil mask nil radius 0 shrink number, min 0 0 title onSet \"\" titleColor transform, onSet 4279310375 0xFF111827 Parts (node:getPart / node:container) closeButton · content · header local w = ui:window({ x=40, y=40, width=360, height=300, title=\"Settings\" }) ui:add(w) w:on(\"close\", function(n) n.visible = false end) w:container():addChild(ui:button({ x=10, y=10, width=100, height=28, text=\"Hi\" })) w.closeButtonText = \"×\" -- themeable glyph w.closeButtonVisible = false -- hide the chrome button ui:checkbox(props) Checkbox Toggling checkbox with a square indicator and a label. Events: change property spec default blur 0 borderColor transform 4291941851 0xFFD1D5DB borderWidth 1 boxColor transform 4294967295 0xFFFFFFFF checked onSet false checkedColor transform 4294967295 0xFFFFFFFF effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 indent 18 justify nil mask nil radius 4 shrink number, min 0 0 textColor transform 4279310375 0xFF111827 local cb = ui:checkbox({ x=0, y=0, width=160, height=20, text=\"Remember\" }) cb:on(\"change\", function(_, on) intro = not on end) ui:radiobutton(props) RadioButton Radio option; use a RadioGroup for mutual exclusivity. Events: change property spec default blur 0 borderColor transform 4291941851 0xFFD1D5DB borderWidth 1 checked onSet false dotColor transform 4294967295 0xFFFFFFFF effect nil flexDirection validate nil gap number, min 0 0 grow number, min 0 0 indent 18 justify nil mask nil shrink number, min 0 " },
    { u: "index.html", t: "Overview", x: " DXUI V4 · retained-mode UI for MTA:SA Widgets, a real theme engine, live translations, pooled retained-mode rendering and headless tests — pure Lua 5.1 on DirectX 9. Quick start local ui = exports.dxui:getUI(\"app\", { design = { width = 800, height = 600 } }) local win = ui:window({ title = \"Hello\", x = 40, y = 40, width = 360, height = 300 }) ui:add(win) win:on(\"close\", function(n) n.visible = false end) win:container():addChild(ui:button({ x = 10, y = 10, width = 100, height = 28, text = \"OK\" })) ui:setTheme(\"dark-compact\") -- any time, live re-apply ui:setLocale(\"ru\") -- your own dictionaries, live re-translate The engine owns the frame loop, input glue and per-resource cleanup — the quick start covers the rest. Why this engine Retained, pooled rendering — one mutation layer with owner guards and category dirty flags; idle frames do zero rebuild/layout work (assert-tested), screen culling, RT compositing only where required. Theme engine — design tokens, 9 built-in themes (3 palettes × 3 densities), custom themes with extends from any resource, per-state styles with opt-in transitions. Live translation — per-resource locale tables, setTextKey bindings, instant switching. Real inputs — hover/focus/press/drag/modal/popup dispatcher, hover states wired for every widget, caret editing with selection, clamped window drag. Sane rendering — one shared SDF rounded-rect shader: per-corner radii, border+fill in a single draw, square corners bypass the shader entirely. Headless tests — the entire engine runs outside MTA with an injected backend; the shipped behavior is assert-locked. Where to read page content Quick start install, first window, the 5-minute tour Concepts nodes, properties, ownership, events, states, parts Widgets every widget: reflected property specs, parts, events, examples Themes registered themes, token vocabulary, defining your own Translate locales, bindings, live switching Edit the text field deep dive: caret, selection, masking Settings every engine key and what consumes it Layout positioning modes, dimensions, anchors, flex Animation animate, easing, transitions, chaining Rendering frame pipeline, primitives, custom widgets, overlays Diagnostics counters, the zero-work idle contract FAQ common questions Migration V3 → V4 every breaking change Repository layout source/settings.lua engine-wide behavior keys source/client/core/ node, widget, parts, value objects source/client/style/ tokens, theme engine, built-in themes source/client/render/ renderer, pass, backend (the ONLY dx* calls), effects source/client/input/ dispatcher, hit-test, events source/client/layout/ dimension/flex/absolute layout passes source/client/api/ runtime, ui handle, exports, diagnostics source/client/widgets/ the widget library demo/ standalone showcase resource meta.xml MTA manifest (script order = dependency order) " },
    { u: "quickstart.html", t: "Quick start", x: " Quick start From zero to a running window in two files. 1. Install Copy the dxui resource into your server's resources folder and start it (the engine ships as ONE resource). In YOUR resource's meta declare the dependency: &lt;include resource=\"dxui\" /&gt; 2. First window local ui = exports.dxui:getUI(\"app\", { design = { width = 800, height = 600 } }) local win = ui:window({ title = \"Hello\", x = 40, y = 40, width = 360, height = 300 }) ui:add(win) win:on(\"close\", function(n) n.visible = false end) local btn = ui:button({ x = 10, y = 10, width = 100, height = 28, text = \"OK\" }) win:container():addChild(btn) btn:on(\"click\", function() win:animate({ opacity = 0.4 }, 150) -- everything is a property end) That is the entire setup: the engine owns the frame loop, the input glue, the design→screen mapping and per-resource cleanup. When your resource stops, its UI instances and assets are released automatically. 3. The 5-minute tour Properties are the API — btn.text = \"Save\" , panel.color = \"@color.primary\" , win.visible = false . Every write is validated and coalesced; nothing repaints needlessly. Events — node:on(\"click\", fn) ; the full vocabulary lives in Concepts . Themes — ui:setTheme(\"dark-compact\") restyles every live widget instantly; define your own with ui:defineTheme . Text — bind labels to locale keys with setTextKey and switch languages live. Run the demo — copy demo/ next to dxui, start it: every widget, live themes, locales and a settings panel. " },
    { u: "concepts.html", t: "Concepts", x: " Concepts nodes, properties, events, states, parts Five ideas carry the whole engine. Node tree Everything visible is a Node in a tree owned by the UI instance ( ui.root ). ui:add(node) mounts a top-level node; node:addChild(c) nests. Children inherit clipping, opacity and the translation/theme context. Destroy cascades. One mutation layer Every property write — node.x = 10 , a setter, an animation tick, the layout pass, a theme switch — funnels through Node:_set : validate (spec type/min/max/validate), then transform (colors, dimensions); old == value → early out (write-backs and engine writes are free and recursion-safe); ownership : user &gt; system &gt; theme — a themed value never overwrites something you set; category invalidation (LAYOUT / RENDER / ORDER / VISIBILITY / INPUT), coalesced into instance flags drained once per frame. Events event handler args when click (node, button, x, y) press+release on the same node, no drag; button is \"left\" / \"middle\" / \"right\" mousedown / mouseup , press / release (node, button, x, y) raw press/release (bubbling) hover-start / hover-end (node) pointer enters/leaves the node pointer-move (node, x, y) opt-in continuous position: set node._hasPointerMove = true ; fires while hovered (GridList uses it for row hover) drag-start / drag-move / drag-end (node, x, y) press moved beyond 6px; drag routes to the pressed node scroll (node, wheel) wheel over the node (bubbles to scrollables) key (node, keyName, isDown, ...) focused node receives keys (shift modifier appended) character (node, ch) printable input for the focused node focus / blur (node) focusable nodes gain/lose dispatcher focus change / submit / select / close / popup-close / localeChange widget-specific see Widgets Handlers bubble to ancestors; returning DXUI.STOP halts propagation. node:on(ev, fn, id) tags a group; node:off(ev, fn) detaches. Visual states Every widget is wired at construction: hover/press pointer transitions map to node:setState(\"hover\"/\"pressed\") , activating the theme's state blocks. Focused is sticky — while a node is focused (an Edit being edited) hover/pressed transitions skip it until its blur handler runs. Release keeps hover while the pointer rests on the node. Disabled nodes map to the themed disabled state automatically. Parts and container() Composites own named slots: window.header/content/closeButton , combobox.head/dropdown , contextmenu.list , … node:getPart(name) reads one; node:container() is the unified accessor for the content slot — children go there. Window title and close glyph are properties ( title , closeButtonText ) routed to their parts. Base property vocabulary property meaning x, y, width, height design-space box (see Layout ) visible, opacity visibility (hiding also leaves the hit-test list), 0..1 alpha color, textColor packed 0xAARRGGBB or \"#hex\" or ui:color(r,g,b,a) ; node.color.r = 255 works font handle from ui:font(path, size) ; nil falls back to the system font then the engine default layer, zIndex paint order (layer &gt; zIndex &gt; insertion) clip, clipMode child clipping (\"rt\" forces the render-target path) margin, padding outer spacing / inner content inset interactive, focusable, enabled input surface switches textKey translation binding (see Translate ) style named theme variant of the widget (\"secondary\", \"ghost\", ...) userData untouched consumer payload " },
    { u: "theme.html", t: "Themes", x: " Themes tokens, presets, custom themes Themes are DATA, not code: a token table (referenced as @color.primary ) plus per-component styles. Built-ins ship in three palettes (light, dark, green) and three densities (normal, -compact, -full). Any resource can define its own theme and extend a built-in. Registered themes (reflected) dark · dark-compact · dark-full · green · green-compact · green-full · light · light-compact · light-full Base token vocabulary light, reflected category tokens color border 0xFFD1D5DB danger 0xFFDC2626 onPrimary 0xFFFFFFFF overlay 0x66000000 primary 0xFF2563EB primaryHover 0xFF1D4ED8 primaryPressed 0xFF1E40AF success 0xFF16A34A surface 0xFFFFFFFF surfaceAlt 0xFFF3F4F6 text 0xFF111827 textSecondary 0xFF6B7280 tooltipBg 0xFF111827 warning 0xFFD97706 padding control {bottom=0, left=8, right=8, top=0} radius full 0x000003E7 lg 0x0000000C md 0x00000008 sm 0x00000004 size headerHeight 0x0000001C rowHeight 0x00000016 sliderThumb 0x0000000E thumb 0x00000008 Component styles Every widget has a themed section. Inside a theme table: ui:defineTheme(\"brand\", { extends = \"dark\", -- optional parent (deep merge) tokens = { color = { primary = 0xFF22D3EE } }, components = { button = { base = { textColor = \"@color.onPrimary\" }, -- @token refs states = { hover = { color = \"@color.primaryHover\" } }, }, window = { props = { radius = 10 } }, }, }) props / base are aliases (both accepted; child base wins over parent props). variants = named styles selected with node.style (\"secondary\", \"danger\", \"ghost\"). Asset prefixes: \"texture:icons/x.png\" and \"font:Roboto.ttf:12\" load cached resources for themed values. State sections apply through the pointer wiring (hover/pressed/focused/selected/disabled); an opt-in transition = { duration, easing } animates state changes (colors lerp per channel). ui:setTheme(name) re-applies to every live widget instantly; mounted-later widgets adopt the active theme on mount. Ownership: user &gt; system &gt; theme — a themed value never overwrites something the consumer set. " },
    { u: "translate.html", t: "Translate", x: " Translate per-resource locales, live switching Each resource registers its OWN dictionaries and switches its instance locale at runtime — no engine-wide dictionary, no restart. ui:addLocale(\"en\", { greeting = \"Hello, %1!\", save = \"Save\" }) ui:addLocale(\"ru\", { greeting = \"Привет, %1!\", save = \"Сохранить\" }) ui:setLocale(\"ru\") local l = ui:label({ x = 10, y = 10 }) l:setTextKey(\"greeting\") -- auto re-translated on every switch label.text = ui:tr(\"greeting\", \"World\") -- one-off lookup, %1 substituted node:setTextKey(key, target?) binds the text property by default ( target picks another text property, e.g. the Edit placeholder or the Window title ). Bindings re-resolve on ui:setLocale and on MOUNT (a binding made before attach resolves against the instance locale as soon as the node mounts). Fallback chain: exact locale → base locale ( ru-RU → ru ) → the key itself. The instance locale ( ui:setLocale ) overrides the engine locale ( DXUI.setLocale ); unset instances follow the engine locale. ui.root:on(\"localeChange\", fn) (or ui:on ) reacts to switches. " },
    { u: "edit.html", t: "Edit", x: " Edit the text field deep dive A single-line input with a real caret: blinking (overlay — never invalidates the render cache), selection, masking, live constraints. local ed = ui:edit({ x = 10, y = 10, width = 220, height = 26, placeholder = \"Name\" }) ed:on(\"submit\", function(_, text) save(text) end) -- Enter keeps focus ed:on(\"change\", function(_, text) ... end) ed:on(\"blur\", function() ... end) -- Escape blurs Properties (reflected) property spec default alignment validate \"left\" bgColor transform 4294967295 0xFFFFFFFF blur 0 borderColor transform 4291941851 0xFFD1D5DB borderWidth 1 caret number, onSet 0 caretBlinkInterval validate nil caretColor transform 4280640491 0xFF2563EB caretMode validate \"blink\" caretWidth number, min 1 1 effect nil flexDirection validate nil focusBorderColor transform 4280640491 0xFF2563EB gap number, min 0 0 grow number, min 0 0 justify nil mask nil maskChar \"*\" masked false maxLength number, min 0 0 placeholder \"\" placeholderColor transform 4285231744 0xFF6B7280 placeholderVisibleWhenFocused false radius 4 readOnly false selectionColor transform 858088427 selectionFrom validate, onSet nil shrink number, min 0 0 textColor transform 4279310375 0xFF111827 Keyboard model key behavior printable insert at the caret (replaces the selection) Left / Right move the caret (Shift extends the selection) Home / End jump to start/end (Shift selects) Backspace / Delete remove before the caret / forward; a selection is removed first Enter emits submit with the text; keeps focus Escape releases focus Caret details caretMode : \"blink\" (default, half-period caretBlinkInterval or the settings default) · \"solid\" · \"off\" . Blinking is a per-frame OVERLAY: zero render-list rebuilds. Click positions the caret at the clicked character (byte index); the text scrolls to keep the caret visible under overflow. masked renders maskChar per byte (the caret/selection still operate on the real text). placeholder hides on focus by default; opt back in with placeholderVisibleWhenFocused = true . selectionFrom + caret bracket the selection; selectionColor is themeable. " },
    { u: "settings.html", t: "Settings", x: " Settings engine-wide behavior (reflected) Appearance lives in themes; behavior lives here. Merge a partial table at any time — every key below is consumed by the engine. key default (reflected) consumed by dev false validation on every property write; warn on misuse errorPolicy \"warn\" \"error\" | \"warn\" | \"ignore\" — failing event handlers defaultTheme \"light\" theme activated at bootstrap and when this key is applied designResolution {mode=stretch} default design space for new UI instances; mode stretch|fit defaults.animationDuration 250 node:animate() default duration (ms) defaults.animationEasing \"inout\" default easing name defaults.scrollWheelStep 48 ScrollPanel wheel travel (px) defaults.caretBlinkInterval 500 Edit caret blink half-period (ms; the widget's caretBlinkInterval overrides) defaults.font nil system font behind every fontless text draw (\"path\" or \"path:size\"; node font &gt; themed font &gt; this) resourcePolicy.autoRelease true release consumer UIs/assets when their resource stops performance.screenCulling true skip render items fully outside the screen performance.maxInteractiveScan 2000 hit-test scan cap (topmost first) performance.renderPriority \"normal\" onClientRender priority of the frame loop; live re-registration DXUI.applySettings({ dev = true, defaultTheme = \"dark-compact\", defaults = { caretBlinkInterval = 400, font = \"Roboto.ttf:14\" }, }) ui:applySettings(t) is the per-instance shortcut. Note: the merge cannot carry nil (Lua table semantics) — reset a raw key via DXUI.Settings.defaults.font = nil if needed. " },
    { u: "layout.html", t: "Layout", x: " Layout boxes, modes, anchors, flex Layout runs ONLY when something dirty requires it — a single two-pass resolve per change. Positioning modes ( layoutMode ) mode semantics absolute (default) x/y/width/height are design pixels inside the parent's content box relative x/y are FRACTIONS of the parent content size (0.5 = center); sizes via layoutWidth/Height center the node centers itself in the parent content box stretch stretches to the parent content size minus margins fill participates in flex flow (see below) Dimensions width/height — direct pixels; layoutWidth/layoutHeight — compiled dimensions: ui:percent(100) , ui:auto() (content-measured), ui:fill() (flex share); autoSize — text widgets measure themselves (labels, buttons). Anchors ( anchor ) A two-letter code from t/op/m/c + l/e/m/c : \"tl\" , \"tr\" , \"mc\" , \"me\" (middle-end) … The box is pulled back inside its own footprint along the anchor: a node with x = 1 (relative) and anchor = \"tr\" pins flush to the parent's top-right corner (the window close button does exactly this). Flex flow Set children to layoutMode = \"fill\" and give the container flow = \"row\" (default) / \"column\" with gap : siblings share the main axis (weighted by ui:fill(n) ) and stretch on the cross axis. layoutWidth = ui:fill() without a flow container = share of the remaining space. Spacing margin (outer) and padding (inner content inset) are tables: { left = 10, right = 10, top = 4, bottom = 4 } or a single number for all sides. Examples -- a header row pinned under a 28px strip (window pattern) local content = win:container() content.padding = { top = 32, left = 10, right = 10 } -- centered dialog local md = ui:modal({ width = 280, height = 120 }) -- flush top-right pin btn.layoutMode, btn.x, btn.y, btn.anchor = \"relative\", 1, 0, \"tr\" " },
    { u: "animation.html", t: "Animation", x: " Animation property tweens through the mutation layer Animations write REAL properties through the mutation layer (owner \"system\"): validation, ownership and dirty flags apply to every tick. win:animate({ y = 90 }, 350, \"out\") -- duration ms, easing name or fn :after({ y = 40 }, 250) -- chain when the first finishes local a = panel:animate({ opacity = 0.3 }, 200) a:pause(); a:resume(); a:cancel() a:onDone(function() print(\"done\") end) Numbers tween directly; colors lerp PER CHANNEL (0xAARRGGBB packed). Defaults come from settings: defaults.animationDuration , defaults.animationEasing . A user write to an animated property revokes the animation's ownership of that property (last writer wins). Theme state transitions ( transition = { duration, easing } in a component) ride the same machinery — opt-in per component, instant by default. Easing library (reflected) back · backIn · backInOut · backOut · bounce · bounceIn · bounceInOut · bounceOut · elastic · elasticIn · elasticInOut · elasticOut · in · inout · linear · out " },
    { u: "render.html", t: "Rendering", x: " Rendering pipeline, primitives, custom widgets One cached, pooled item list; the tree is re-collected ONLY when dirty. Idle frames draw the cache and do nothing else. Frame pipeline tick() animations update (early-out when idle) layout IF layoutDirty (walks the tree, writes via _set \"system\") rebuild IF renderDirty (collect → sort → emit into the pooled list) hit-test rebuild IF interactiveDirty draw the cached list overlays (per-frame clock repaints; no invalidation) Renderer primitives inside render(renderer) call params (design space) renderer:rect(x, y, w, h, color) solid rectangle renderer:roundedRect(x, y, w, h, radii, color) radii: number | {lt,tr,br,bl} renderer:borderedRect(x, y, w, h, radii, fill, border, borderWidth) ONE SDF draw: border ring + fill, per-corner radii, 1px AA; square corners decompose to plain rects renderer:text(str, x, y, w, h, color, font, align, valign, scale) native text alignment; fontless draws fall back to the system font renderer:image(tex, x, y, w, h, color, rot?, rotX?, rotY?, section?) texture quad (sections in pixels) renderer:line(x1, y1, x2, y2, color, width) line segment Rounded corners ONE shared SDF shader process-wide (ps_2_0): per-corner radii, border and fill in a single draw, 1px smoothstep AA. Parameters dedupe on consecutive draws; square corners bypass the shader entirely. The shader file lives inside the dxui resource and compiles once at bootstrap. Effects node.blur (px) and node.mask (texture path) on any widget or container; containers composite into an RT group automatically when needed. clipMode = \"rt\" forces the RT path. Identical effect inputs dedupe to one shader instance (the Effects cache). Overlays Widgets that must repaint every frame from the clock (the Edit caret blink) implement node:overlay(renderer) instead of invalidating: the runtime re-emits overlays right after the cached list through the same backend. The zero-work idle contract holds — blinking costs zero rebuilds. Writing a custom widget Extend DXUI.Widget , declare property specs, implement render(renderer) with primitives only (no dx* calls), and register it — registration synthesizes the factory automatically. local Badge = DXUI.Widget:extend(\"Badge\", { count = { default = 0, type = \"number\", min = 0, invalidates = { DXUI.DIRTY.RENDER } }, badgeColor = { default = 0xFFEF4444, invalidates = { DXUI.DIRTY.RENDER }, transform = DXUI.resolveColor }, }) function Badge:render(renderer) renderer:borderedRect(self.worldX, self.worldY, self.width, self.height, self.radius or 0, self.color, self.borderColor, self.borderWidth) renderer:text(tostring(self.count), self.worldX, self.worldY, self.width, self.height, self.textColor, self.font, \"center\", \"center\", 1) end DXUI.Builders.register(\"Badge\", Badge) -- ui:badge({...}) now exists " },
    { u: "diagnostics.html", t: "Diagnostics", x: " Diagnostics counters + the zero-work idle contract The engine measures itself; the test suite assert-locks the idle behavior. DXUI.Diagnostics.enableZeroWork(ui, true) -- HARD-assert idle frames do zero work local snap = DXUI.Diagnostics.snapshot(ui) -- frames/layoutRuns/rebuilds/hitRebuilds/items/draws print(DXUI.Diagnostics.describe(ui)) -- one-line summary DXUI.Diagnostics.idleRatio(ui) -- idle frames fraction; >0.9 is healthy DXUI.Diagnostics.report(ui) -- verbose per-counter report The zero-work idle contract An idle frame (no writes, no pointer transitions) performs NO layout, NO rebuild, NO hit-test rebuild — it draws the cached list and nothing else. Hover transitions repaint — they are REAL visual changes (themed state colors) — and coalesce: many transitions inside one frame still cost ONE rebuild. The caret blink NEVER rebuilds (overlay path): 100 idle frames of blinking = zero rebuilds, verified by tests. One property write = exactly one rebuild; one text write = one layout + one rebuild. Reading the numbers counter meaning frames ticks executed layoutRuns layout passes (dirty-driven) rebuilds render-list rebuilds hitRebuilds interactive-list rebuilds items items in the cached list draws backend draw calls total " },
    { u: "faq.html", t: "FAQ", x: " FAQ Common questions, short answers. How do I change the caret blink speed? Per widget: ed.caretBlinkInterval = 250 . Engine-wide: ui:applySettings({ defaults = { caretBlinkInterval = 250 } }) . caretMode = \"solid\" disables blinking entirely. How do I change the font everywhere? ui:applySettings({ defaults = { font = \"Roboto.ttf:14\" } }) — the system font behind every draw without an explicit font. A themed font on a component still wins; a per-node font wins over both. How do I hide the window close button? win.closeButtonVisible = false . The glyph is win.closeButtonText (a themed Button part — restyle it through the button theme component). Why does my themed hover state not show? Hover styling comes from the theme's states.hover block — check it exists for the component and the node is interactive (or subscribes to input events). Focused nodes deliberately keep their focused styling until blur. Popups vs modals — which one? Modal blocks ALL outside input (dialog semantics, stacked). Popup / ContextMenu /the ComboBox dropdown close on the first outside click. Use modals for confirmations, popups for transient anchored UI. How do row-hover highlights work in GridList? GridList rows are data, not child nodes; the list sets node._hasPointerMove = true and receives continuous pointer-move events while hovered, invalidating render only when the hovered ROW index changes. Combobox/contextmenu rows are real child widgets and hover through the normal hover events. Can I run the engine headless / write tests? Yes — the engine is pure Lua 5.1 outside two files (backend + init). Inject a table backend via DXUI.Runtime.backend , a fake clock via the clock option, and drive input through ui:mouse*/key/character . The shipped suite works exactly this way. Where is the frame loop? init.lua registers ONE onClientRender handler (priority from settings.performance.renderPriority ). ui:tick() is public if you want to drive frames yourself. " },
    { u: "migration.html", t: "Migration V3 → V4", x: " Migration V3 → V4 V4 is a breaking release. Every public change, by area. Edit cursor property renamed to caret . New: caretWidth , caretMode (\"blink\"|\"solid\"|\"off\"), caretBlinkInterval , selectionFrom / selectionColor , alignment , maxLength , readOnly , masked / maskChar , placeholderVisibleWhenFocused . Click positions the caret (was: focus always moved it to the end). Escape blurs; Enter submits keeping focus; Delete works; overflow scrolls to keep the caret visible. Naming DXUI.EASING → DXUI.Easing . Key event second parameter pressed2 → isDown (signature (keyName, isDown, ...) ; the shift modifier is appended). Removed dead API: DXUI.Values , Part.themeRole , Part.replace (use node:setPart ). tx_is_separator → isSeparator (internal). Themes Built-in theme \"default\" renamed to \"light\" ; 9 built-ins via density presets ( dark-compact , green-full , …). ui:defineTheme(name, tbl) / ui:setTheme(name|table) ; deep-merge extends ; asset prefixes ( texture: , font: ); opt-in transition blocks per component. Live switching re-applies to EVERY live widget (better than DGS — created widgets repaint too); mounted-later widgets adopt on mount. Factories ui:&lt;widget&gt;() factories are SYNTHESIZED from the widget registry — every registered class gains one ( ui:radiogroup() appeared). The hardcoded list is gone. Every composite exposes container() (window, scrollpanel, tabpanel, combobox, contextmenu, modal, popup). Window / composites New closeButton part — a real themed Button — plus closeButtonText and closeButtonVisible (click emits \"close\" ). The header drags the window (clamped on-screen; draggable gates). TabPanel pages parent to the content part; ComboBox open is a real driving property (writes sync the dropdown part + popup registry). Rendering drawRoundedRect(x, y, w, h, rtl, rtr, rbr, rbl, fill, border, borderWidth) — new signature; render-list rrect items carry per-corner radii + border fields; ONE SDF shader process-wide. Removed: Effects.round , Effects.whiteTexture , Renderer.resolveEffect . Fontless text draws fall back to the system font ( settings.defaults.font ). Input Hover/pressed states are wired at construction for every widget (theme state blocks engage through pointer input); focused is sticky. New opt-in pointer-move event (continuous position for hovered opt-ins — row-level hover in data-driven widgets). visible invalidates the input set too — hidden interactive nodes leave the hit-test list on the next collect. DXUI.setRenderPriority re-registers the frame loop; the mouse wheel falls back to the screen center when the cursor is disabled. " }
];