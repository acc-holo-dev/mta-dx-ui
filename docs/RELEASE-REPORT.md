# Аудит и подготовка к релизу — DXUI v2 (mta-dx-ui)

Дата: подготовка релиза 2.1.0. Статус: **готово к релизу** (после фиксов в этом документе).

---

## 1. Резюме

Кодовая база в хорошем состоянии: чистая архитектура (AoS-ноды, единый
mutation layer, именованные dirty-категории, плоские списки для render/hit-test),
все 15 тестовых файлов зелёные, весь код синтаксически валиден под Lua 5.1
(как в MTA). Однако проект **не был готов к релизу** по нескольким причинам:

1. **Критично**: в репозитории лежали незакоммиченные фиксы рантайм-моста
   MTA (init.lua, dispatcher.lua, context.lua, backend_mta.lua) — то есть
   закоммиченная версия 2.1.0 при реальном запуске в MTA была сломана:
   - `init.lua` вызывал серверную `getScreenSize()` (в клиенте такой функции
     нет, клиентская — `guiGetScreenSize()`);
   - обработчик `onClientKey` передавал булево `press` как `state`, из-за чего
     события клавиш/text в игре не работали вовсе (мод-фикс уже лежал в дереве);
   - колесо мыши (`mouse_wheel_up/down`) не обрабатывалось;
   - бэкенд применял шейдер неправильно — в аргумент `postGUI` функции
     `dxDrawImage` (10-й параметр), а не как материал (5-й). Проверено по
     официальной вики MTA: материал-шейдер передаётся пятым аргументом.
2. **Критично**: даже после этих фиксов комбинации Ctrl (Ctrl+A/C/V/X в Edit)
   не работали бы в игре — `mods` всегда передавался пустым (`""`), т.к.
   `onClientKey` не отдаёт модификаторы. Добавлено отслеживание модификаторов
   в мосту.
3. **Версия**: `meta.xml` сообщал `2.0.0`, хотя коммит — 2.1.0. Не согласовано.
4. **Документация**: в README и шапке translation.lua пример локали
   `{ "menu.open" = "..." }` — синтаксическая ошибка Lua (строковый литерал
   как record-key); корректно `{ ["menu.open"] = "..." }`. Список виджетов в
   README противоречил сам себе (23 vs 24, упомянут не-виджет `tooltip`,
   пропущен внутренний `toggle`). Quick-start советовал добавлять свой
   `onClientRender`, что приводило бы к двойной отрисовке (init.lua рендерит
   все контексты сам). Дерево файлов в ARCHITECTURE.md устарело (нет 7 виджетов,
   тестов m23–25, есть несуществующий `demo.lua`).
5. **Пробелы в тестах**: не покрывались новый путь ввода `onCharacter`
   (MTA `onClientCharacter` → текст в Edit) и ctrl-шорткаты Edit.
6. **Инфраструктура git**: не было `.gitattributes` (на Windows autocrlf
   порождал CRLF-шум), не было тега релиза.

---

## 2. Методика проверки

- Запуск полного набора тестов: `python tests/run.py` (15 файлов, 382+ asserts,
  lupa/Lua 5.1 — тот же диалект, что в MTA).
- Синтаксическая проверка всех 64 `.lua`-файлов через `loadstring` в Lua 5.1.
- Сверка `meta.xml` ↔ `tests/loader.lua` ↔ реальных файлов (48/48 клиентских
  модулей в манифесте; loader намеренно пропускает только init.lua/export.lua).
- Ручной ревью всех модулей client/ (core, api, input, layout, render, style,
  text, animation, resources, utils, 25 widgets).
- Сверка фактов API MTA с официальной вики: `onClientKey(button, press)`,
  колесо через `mouse_wheel_up/down` (без release), `onClientCharacter(char)`,
  `dxDrawImage(image=material...)` — перекрёстно подтверждено.

---

## 3. Найденные проблемы и их статус

| # | Крит. | Проблема | Где | Статус |
|---|-------|----------|-----|--------|
| 1 | Critical | Клиентский ввод сломан в закоммиченной версии (getScreenSize, onClientKey, wheel) | client/init.lua | ✅ исправлено (фиксы доведены и проверены) |
| 2 | Critical | Шейдеры применялись неверно (postGUI вместо материала) | client/render/backend_mta.lua | ✅ исправлено |
| 3 | Critical | Ctrl-шорткаты Edit мертвы в игре (mods пустой) | client/init.lua | ✅ добавлен трекер модификаторов |
| 4 | Major | Версия в meta.xml = 2.0.0, в git — 2.1.0 | meta.xml | ✅ 2.1.0 |
| 5 | Major | Невалидный Lua-пример локали | README.md, client/translation.lua | ✅ исправлено |
| 6 | Major | Несогласованный список виджетов (23 vs 24, toggle/tooltip) | README.md | ✅ исправлено |
| 7 | Major | Quick-start вызывает двойную отрисовку | README.md | ✅ исправлено |
| 8 | Major | Устаревшее дерево файлов | ARCHITECTURE.md | ✅ обновлено |
| 9 | Major | Нет тестов на onCharacter и ctrl-шорткаты | tests/ | ✅ добавлено (+11 asserts) |
| 10 | Minor | Нет .gitattributes → CRLF-шум | — | ✅ добавлен |
| 11 | Minor | Нет тега релиза | git | ✅ создан тег v2.1.0 |
| 12 | Minor | Нет examples/ (leftover ROADMAP) | examples/ | ✅ добавлен demo |
| 13 | Minor | ROADMAP упоминает «перевести ADR 001–006» — они уже на английском | ROADMAP.md | ✅ статус обновлён |

Критических дефектов в самой библиотеке (core/widgets) не обнаружено: ядро,
виджеты и события чисты, деструкторы составных виджетов (window-modal, popup,
combobox-dropdown, radiobutton-group) корректно чистят диспетчерские стеки.

---

## 4. План работ (выполнен)

1. Зафиксировать и довести рантайм-фиксы MTA-моста и бэкенда (были в working tree).
2. Добавить отслеживание модификаторов (ctrl/shift/alt) в onClientKey-мост;
   отсев mouse-кнопок (они приходят через onClientClick).
3. Согласовать версию 2.1.0 в meta.xml; добавить .gitattributes (LF).
4. Исправить документацию: README (quick start, виджеты, локали), translation.lua
   (комментарий), ARCHITECTURE.md (дерево).
5. Добавить тесты: dispatcher/context:onCharacter, ctrl+A/C/V/X для Edit.
6. Создать examples/demo.lua + meta.xml.
7. Полный прогон тестов, синтаксис-чек, сверка манифестов.
8. Коммит + тег v2.1.0.

---

## 5. Верификация (итоги)

- `python tests/run.py` → **15/15 PASSED, 393 asserts** (было 382; +11 новых).
- Синтаксис: **64/64 .lua файлов валидны** в Lua 5.1.
- `meta.xml` ↔ loader ↔ файлы: 48/48 клиентских модулей в манифесте, порядок
  загрузки совпадает с loader.lua.
- Поведение рантайм-путей (onCharacter → текст в Edit, readonly/no-focus guard,
  wheel → scroll) подтверждено дополнительным smoke-прогоном.

---

## 6. Остаточные риски (не блокируют релиз)

- Полное поведение в игре (шейдеры DX9, RT-группы, курсор) невозможно
  проверить за пределами MTA; тесты покрывают логику через мок-бэкенд.
  Перед деплоем желательно 5 минут smoke-теста в MTA: окно + кнопка + Edit
  (ввод + Ctrl+A/C/V/X + колесо) + scrollpanel.
- `getUI()` при старте ресурса — известная особенность MTA (нужен setTimer),
  задокументирована в README и примере.
- Файлы, изменённые при подготовке этого релиза, закоммичены отдельным
  коммитом 2.1.0; тег `v2.1.0` создан.

---

## 7. Быстрая проверка после клонирования

    python tests/run.py                     # все 15 файлов зелёные
    python tests/run.py test_input.lua      # один файл